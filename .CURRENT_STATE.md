# SonicSentinel AI — Verified Current State (as of 2026-09-23 22:24)

This file was written by the coordinator after **directly inspecting the disk**. Trust it over
any agent's claim. Numbers below are real, counted on disk.

## Workspace
`/home/ammar/Downloads/SRS_NextWave AI and ML/sonicsentinel-ai`
Backup of this exact state: `.backup_sonicsentinel_222423` (parent dir).

## What exists and works
- **Python venv present** at `.venv/` — torch 2.14.0+cpu, scikit-learn 1.9.1, numpy 2.5.3,
  librosa 1.0.0, soundfile 0.14.0. `requirements.txt` and `conftest.py` at repo root.
- **61 Python files** (excluding venv and `__pycache__`).
- **Mandated SRS folders present**: `alert_rules/ audio_dataset/ audio_preprocessing/ config/
  data/ database/ documentation/ feature_extraction/ python_models/ src/ static/ templates/
  tests/ tools/`.
- `src/services/`: `config.py`, `pipeline.py`, `ratelimit.py`, `search.py`.
- `src/models.py` (10 tables), `src/db.py`.
- `python_models/`: `classical.py`, `dataset.py`, `train_classical.py`, `tuning.py`.
- `tests/`: **9 test files**. `database/`: `init_db.py`, `schema.sql`, `seed_credentials.json`.
- `documentation/api_contract.md` (~429 lines, v1.0.0), `documentation/perception/`.
- `alert_rules/`: `alert_rules.json`, `manual_review_conditions.json`, `severity_levels.json`,
  `retention.json`, `README.md`.
- `templates/` + `static/`: **17 files**.

## Audio dataset — the real numbers
- `audio_dataset/originals/`: **1,150 wav files** across the 10 classes, 100–150 per class.
- `audio_dataset/raw_downloads/fsd50k_audio/`: **1,150 wav files** downloaded but NOT yet
  curated or classified.
- **SRS requires ≥3,000 unique originals (300/class).** So the curated set is ~38% of target.
  The raw FSD50K pool is the reservoir to close that gap. **This is the biggest data risk.**
- `audio_dataset/synthetic/`: **0 wavs** — augmentation pipeline never produced files.

## Splits — WARNING: BOTH ON DISK ARE STUBS, DO NOT TRAIN ON THEM
- `audio_dataset/manifest_with_split.csv`: **only 120 rows** (12/class; train 80 / val 20 /
  test 20). This is a pilot stub, nowhere near the required 2100/450/450.
- `data/splits/*_ids.txt`: stale stub of ~200 rows, no `split.json`.
- **No frozen canonical split exists yet.** Building it is a day-1 blocker.

## Trained models — NONE
- There are **zero** saved model files on disk: no `.pt`, `.pth`, `.onnx`, `.pkl`, `.joblib`,
  `.h5`, `.keras` anywhere outside the venv.
- Nothing has been trained. The Python model AND the Teachable Machine evidence are both
  still outstanding.

## Git — NONE
- **No `.git` directory.** The SRS requires commits across all five competition days plus a
  dev log. The repo must be initialised and an initial commit made immediately.

## Your inheritance, in one line
Good scaffolding (services, models, schema, contracts, tests, 1,150 curated clips) but
**no models, no real split, not enough data, no git, no TM evidence**.
