# SonicSentinel AI -- Python dependencies
# Python 3.12 required (torch/librosa have no 3.14 wheels on this machine).
# Install: python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
# (this repo was built with uv: uv venv --python 3.12 .venv && uv pip install -r requirements.txt)

# --- core numerics ---
numpy==2.5.3
scipy==1.18.1
pandas==3.0.6

# --- audio I/O + DSP ---
soundfile==0.14.0
librosa==1.0.0
numba==0.67.0
llvmlite==0.49.0
mutagen==1.48.1          # MP3/M4A/FLAC metadata (FR ix: upload metadata)
# ffmpeg (system binary, not pip) handles OGG/M4A transcode -- see README

# --- classical ML ---
scikit-learn==1.9.1
xgboost==3.4.1
joblib==1.6.0

# --- deep models (CPU wheels; nadia) ---
# torch installed from the CPU index: uv pip install --index-url https://download.pytorch.org/whl/cpu torch
torch==2.14.0+cpu

# --- web app ---
Flask==3.1.3
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
gunicorn==26.2.0

# --- config / reporting / charts ---
PyYAML==6.0.3
matplotlib==3.11.2
pillow==12.3.0

# --- testing ---
pytest==9.1.1
